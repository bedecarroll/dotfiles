interface:
  display_name: "OpenAI Image API"
  short_description: "Generate images via the OpenAI Images API script"
  default_prompt: "Use this skill to call scripts/generate_image.py and save generated images with explicit model and output options."
policy:
  allow_implicit_invocation: false
