README.md
install.sh

{{ if not .is_personal }}
# Only add GitHub keys on personal devices
.ssh/authorized_keys
# Probably need to have proxy configs so do these files manually
.curlrc
.gitconfig
{{ end }}

{{ if not .needs_terminal }}
# Terminal emulator configs
.config/kitty/
.config/alacritty/
.config/bash/kitty
{{ end }}

{{ if not (contains "microsoft-WSL2" .chezmoi.kernel.osrelease) }}
# Only configure podman on WSL2
.config/containers/
{{ end }}
